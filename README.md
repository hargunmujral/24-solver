To setup, run the following command: `./setup.sh`

To run the program with a user interface, run the following command: `./run.sh`

To run the program in terminal, run the following command: `python3 solver.py`
